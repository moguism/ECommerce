export class Member {
    image : string;
    name : string;
    description : string;

    public constructor(image: string, name: string, description: string)
    {
        this.image = image;
        this.name = name;
        this.description = description;
    }
}
